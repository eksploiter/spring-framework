<?xml version="1.0" encoding="UTF-8"?>

<!-- sql mapping -->

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.ssafy.shopApp.model.dao.IUserDAO">
	
	<select id="login">
		select name from userinfo where userid=#{userId} and password=#{password}
	</select>
	
	<insert id="register">
		INSERT INTO userinfo (userid, password, name, email) 
		VALUES (#{userid}, #{password}, #{name}, #{email})
	</insert>
	
	<select id="getUserById">
		SELECT userid, password, name, email from userinfo where userid = #{userId}
	</select>
	
</mapper>

<!-- DTD: document type definition -->